// Write a function to calculate factorial of a given number using recursion.
#include<stdio.h>
int fact(int a);
void main()
{
  int x;
  printf("Enter the number\n" )
  scanf("%d",x );

}
int fact(int a)
{
//  for( i =1;i<x;i++)
if(a==0)
{
  printf("factorial 0f %d = %d",a,b)
  return
  int ans,i=0;
}
  ans=ans+fact(i+1)
  printf("the factorial of number %d is : %d",x,fact);
}
